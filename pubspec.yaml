name: flutter_card_scanner_plus
description: >-
  On-device bank card scanner (Visa, Mastercard, Amex). Native OCR via
  iOS Vision and Android ML Kit, no third-party camera plugin required.
version: 0.1.1
repository: https://github.com/PopovVA/flutter_card_scanner_plus
issue_tracker: https://github.com/PopovVA/flutter_card_scanner_plus/issues
topics:
  - camera
  - ocr
  - credit-card
  - payment
  - scanner

environment:
  sdk: ^3.13.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: dev.apissystems.flutter_card_scanner_plus
        pluginClass: FlutterCardScannerPlusPlugin
      ios:
        pluginClass: FlutterCardScannerPlusPlugin
